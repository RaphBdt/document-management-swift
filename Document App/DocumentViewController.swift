//
//  DocumentViewController.swift
//  Document App
//
//  Created by Raphaël Beaudet on 03/01/2024.
//

/*import UIKit
import QuickLook

class DocumentViewController: UIViewController {

    @IBOutlet weak var imageView: UIImageView!
    
    var imageName: String?
    
    override func viewDidLoad() {
        super.viewDidLoad()
        // Do any additional setup after loading the view.
        
        imageView.image = UIImage(named: imageName ?? "defaultImage")
    }
    
    // Implement QLPreviewControllerDataSource methods
    /*func numberOfPreviewItems(in controller: QLPreviewController) -> Int {
        // Return the number of items to preview
        return 1
    }

    func previewController(_ controller: QLPreviewController, previewItemAt index: Int) -> QLPreviewItem {
        guard let url = Bundle.main.url(forResource: String(index), withExtension: "jpg") else {
            fatalError("Could not load \(index).jpg")
        }

        return url as QLPreviewItem
    }*/

    /*
    // MARK: - Navigation

    // In a storyboard-based application, you will often want to do a little preparation before navigation
    override func prepare(for segue: UIStoryboardSegue, sender: Any?) {
        // Get the new view controller using segue.destination.
        // Pass the selected object to the new view controller.
    }
    */

}
*/
